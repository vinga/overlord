/**
 * Review-marker constants shared by the rail and the detail panel.
 * Mirrors `PARK_REASON_MAX` in `packages/server/src/ai/taskStorage.ts` — the
 * server rejects anything longer with a 400, so keep the two in sync.
 */
export const PARK_REASON_MAX = 280;
